export enum QuantityMetric {
  GRAM = 'g',
  KILOGRAM = 'kg',
  MILLILITER = 'ml',
  LITER = 'l',
  PLATE = 'plate',
  HALF_PLATE = 'half_plate',
  EACH = 'each',
}

export enum Role {
  SUPER_ADMIN = 'super_admin',
  ADMIN = 'admin',
  EMPLOYEE = 'employee',
}

export enum RegistrationType {
  PAN = 'PAN',
  VAT = 'VAT',
}
